[package]
name = "mirror-network"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
mirror-protocol = { path = "../mirror-protocol" }

[dev-dependencies]
mirror-crypto = { path = "../mirror-crypto" }
